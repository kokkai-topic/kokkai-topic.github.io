# 国会議論割合可視化サイト 設計書

2026-06-11 ヒアリングセッションでの決定事項。

## コンセプト

国会で議論されている**具体的な争点**（例：ガソリン税減税、政治資金規正法、年収の壁）と、
それが全体の議論量のうち**どのくらいの割合**を占めるかを可視化する一般公開サイト。

- 位置づけ: 個人プロジェクトとして一般公開。小さく作って公開し、反応を見て育てる。

## データソース

- **国会会議録検索システムAPI**（国立国会図書館、無料・認証不要）
  - https://kokkai.ndl.go.jp/api.html
  - 本会議・委員会の全発言テキストを JSON で取得
  - 会議録の公開は開催から数日〜数週間遅れる（許容済み）

## 対象範囲

- **期間**: 現在（直近）の国会会期から開始。過去分のバックフィルは反応を見てから拡張。
- **会議**: 衆参両院・本会議＋全委員会すべて（「全体の割合」の分母として唯一誠実な定義）。

## トピック分類の方式

| 項目 | 決定 |
|---|---|
| 粒度 | 粗い分類（経済・外交）ではなく**具体的な争点レベル**。フラット構造（固定の大分類なし） |
| トピック抽出 | **LLMで完全自動抽出** |
| 名寄せ | **正規トピック台帳方式**：既存台帳（ID・名称・説明）をLLMに渡し、既存へのマップか新規登録かを判定させる。定期的にLLMで台帳掃除（重複統合） |
| 発言への割当 | **LLMが発言単位で主トピックを1つ割当**（1発言＝1トピック）。発言ID→トピックIDの対応表を出力させる |
| 未分類 | どの争点にも該当しない発言は「その他」 |
| 除外 | 議事進行発言（委員長の司会、「速記を止めてください」等）は分母から除外 |

## 割合の測定

- **発言の文字数**ベース（発言時間の代理指標）
- 文字数の集計はコードで機械的に実行（LLMは割当のみ）→ 数字が検証可能
- トピックごとの割合は合計100%

## LLM実行方針

- **当面はAPI課金ではなく Claude Code のサブスクリプション内で実行**
- 初期バックフィル（1会期分・数千万トークン規模）は利用上限を考慮し数日に分けてチャンク処理
- 週次更新: **ローカル半自動**（更新処理一式をスクリプト＋カスタムスキルにまとめ、週1回1コマンドで実行）
  → 安定したら Claude Code の `/schedule`（定期クラウド実行）へ移行

## フロントエンド

- **メイン可視化**: ツリーマップ（面積＝議論量、ECharts内蔵のものを使用）
- **ドリルダウン**: トピック詳細ページ
  - LLM生成の争点サマリー
  - 週次推移グラフ
  - 主な発言者
  - 該当発言一覧（国会会議録検索システムの原文リンク付き → 全数字が一次ソースまで遡れる）
- **フィルタ（v1）**: 期間切替（直近1ヶ月／会期全体／週ごと）＋ 政党（会派）別

## 技術スタック

| レイヤ | 選定 |
|---|---|
| サイト生成 | **Astro**（トピック詳細ページをビルド時に静的生成、SEO・OGP対応） |
| グラフ | **ECharts**（ツリーマップ・推移グラフ） |
| パイプライン | **TypeScript + JSONファイル** |
| データ管理 | 生の会議録はローカルキャッシュ（git除外）。リポジトリには派生データのみコミット：トピック台帳／週次集計／発言インデックス（抜粋＋原文リンク） |
| ホスティング | **GitHub Pages**（データ更新コミット → Actions で Astro ビルド → 自動公開） |

## 未決事項（実装時に決める）

- サイト名・ドメイン
- リポジトリの公開設定（GitHub Pages 無料枠は公開リポジトリが前提）
- 「その他」タイルのツリーマップ上での表示方法
- 政党（会派）マスタの管理方法（会派名の表記揺れ・会期途中の変動への対応）
- 手法解説ページ（メソドロジー）の内容 — 政治データを扱う以上、集計方法の透明性ページを v1 に含めることを推奨

## 既知のリスク

- LLM分類の再現性: 同じ入力でも割当が揺れうる → 割当結果をJSONで永続化し再分類しない運用でカバー
- 会議録公開の遅延: 「リアルタイム」ではないことをサイト上に明記
- 政治的中立性: 分類はすべて機械処理であること・手法を公開していることを明示
